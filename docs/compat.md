# Compatibility matrix

| agent-wizard version | agentskills.schemaVersion | agentskills.lock schemaVersion |
|----------------------|---------------------------|-------------------------------|
| v0.x (dev)           | 1                         | 1                             |

Notes:

- Breaking changes should bump manifest/lock `schemaVersion` and include a `migrate` story.
