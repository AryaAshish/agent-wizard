# Support matrix (policy)

- Maintain **N** and **N-1** minor versions with security/critical bugfixes.
- Default platform targets: macOS, Linux, Windows (best-effort for path edge cases).
- Go toolchain: see `go.mod` `go` directive for the minimum supported toolchain.
