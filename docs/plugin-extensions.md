# Plugin / source extensions (v1)

v1 treats non-core source kinds as built-in capabilities in this repository (not a stable third-party plugin ABI).

A public plugin/hosting API may be introduced after v1 once real-world usage stabilizes.
